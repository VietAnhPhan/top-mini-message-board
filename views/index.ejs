<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=title%>
    </title>
</head>

<body>
    <h1>
        <%=title%>
    </h1>
    <a href="/new">New</a>
    <% messages.forEach((message, index)=> { %>
        <p>User: <%=message.user%>
        </p>
        <p>Text: <%=message.text%>
        </p>
        <p>Date: <%=message.added%>
        </p>


        <a href=<%=`/messages/${index}` %>>View message</a>
        <%}); %>
</body>

</html>